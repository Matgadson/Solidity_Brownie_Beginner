import Web3AuthEmbed, { WS_EMBED_BUILD_ENV, BUTTON_POSITION } from '@web3auth/ws-embed';

const WALLET_URLS_SAFE = {
  [WS_EMBED_BUILD_ENV.TESTING]: {
    url: "https://lrc-safe.web3auth.io",
    logLevel: "debug"
  },
  [WS_EMBED_BUILD_ENV.DEVELOPMENT]: {
    url: "http://localhost:4050",
    logLevel: "debug"
  },
  [WS_EMBED_BUILD_ENV.PRODUCTION]: {
    url: "https://safe.web3auth.io",
    logLevel: "error"
  }
};
class SafeAuthEmbed extends Web3AuthEmbed {
  async init() {
    let {
      buildEnv = WS_EMBED_BUILD_ENV.PRODUCTION,
      enableLogging = false,
      chainConfig,
      showWidgetButton = false,
      buttonPosition = BUTTON_POSITION.BOTTOM_LEFT,
      walletUrls
    } = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
    return super.init({
      buildEnv,
      enableLogging,
      chainConfig,
      showWidgetButton,
      buttonPosition,
      walletUrls: walletUrls || WALLET_URLS_SAFE
    });
  }
}

export { SafeAuthEmbed as default };
//# sourceMappingURL=embed.esm.js.map
