import Web3AuthEmbed, { WsEmbedParams } from "@web3auth/ws-embed";
export default class SafeAuthEmbed extends Web3AuthEmbed {
    init({ buildEnv, enableLogging, chainConfig, showWidgetButton, buttonPosition, walletUrls, }?: WsEmbedParams): Promise<void>;
}
